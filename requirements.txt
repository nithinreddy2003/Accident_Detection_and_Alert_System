streamlit==1.62.0
ultralytics==8.4.33

# Stay on the 4.x line. Unpinned, this resolved to opencv-python 5.0.0.93 — a
# brand-new major release that ultralytics 8.4.33 predates and is not tested
# against. "<5" resolves to 4.14.0.94.
opencv-python>=4.9,<5

numpy
Pillow

# torch is not imported by app.py, but ultralytics needs it to run the model.
# The "+cpu" suffix is the important part: that local version exists only on
# PyTorch's own index, which forces the ~200 MB CPU wheel. Plain "torch" on
# Linux pulls the CUDA build plus multi-GB nvidia-* packages and the hosted
# build runs out of room.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
torchvision==0.21.0+cpu
